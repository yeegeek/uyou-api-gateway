server:
  grpc_port: {{GRPC_PORT}}
  http_port: {{HTTP_PORT}}

database:
  host: mongodb
  port: 27017
  user: root
  password: example
  dbname: {{DB_NAME}}

redis:
  host: redis
  port: 6379
  db: {{REDIS_DB}}
  password: ""

jwt:
  # JWT Secret - 优先从环境变量 JWT_SECRET 读取
  # 如果环境变量不存在，则使用此值（仅用于开发环境）
  secret: uyou_secret_key_2026

log:
  level: info
  format: json
