package model

import (
	"time"

	pb "{{MODULE_PATH}}/api/proto"
)

// {{SERVICE_TITLE}} 数据库模型 (PostgreSQL)
// 对应表: {{TABLE_NAME}}
type {{SERVICE_TITLE}} struct {
	ID        int64     `json:"id" db:"id"`
	Name      string    `json:"name" db:"name"`
	CreatedAt time.Time `json:"created_at" db:"created_at"`
	UpdatedAt time.Time `json:"updated_at" db:"updated_at"`
}

func (m *{{SERVICE_TITLE}}) TableName() string {
	return "{{TABLE_NAME}}"
}

// ToProto 转换为 Protobuf 消息
func (m *{{SERVICE_TITLE}}) ToProto() *pb.{{SERVICE_TITLE}} {
	return &pb.{{SERVICE_TITLE}}{
		Id:        m.ID,
		Name:      m.Name,
		CreatedAt: m.CreatedAt.Unix(),
		UpdatedAt: m.UpdatedAt.Unix(),
	}
}
